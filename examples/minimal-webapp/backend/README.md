# TaskBoard backend (stub)

Example API service owned by `@backend-agent`.

Imagined stack: Node + Express + Postgres.

```bash
# fictional
npm run dev   # http://localhost:4000
curl -s http://localhost:4000/health
```
